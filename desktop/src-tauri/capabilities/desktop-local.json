{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-local",
  "description": "Bundled bootstrap and recovery commands only.",
  "windows": ["main"],
  "permissions": [
    "allow-complete-bootstrap-check",
    "allow-select-project-directory",
    "allow-retry-start-sidecar",
    "allow-quit-app",
    "allow-open-diagnostics-directory"
  ]
}
